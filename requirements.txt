streamlit==1.35.0
google-generativeai==0.7.2
supabase==2.31.0
realtime==2.31.0
websockets==15.0
python-dotenv==1.0.1
google-play-scraper==1.2.7
google-search-results==2.4.2
plotly==5.22.0
jinja2==3.1.4
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
# app-store-scraper removed: pinned requests==2.23.0 which conflicts with streamlit's
# requests>=2.27, forcing pip into a 10+ min backtracking loop that OOMs the build
# container. scraper/app_store.py handles ImportError gracefully — App Store is skipped
# and Play Store + web + Reddit sources top up via the orchestrator's balancing logic.
